How do we go through the challenge obstacle?
We use a light sensor and a distance sensor here when the distance sensor see the unit doing certain actions lightn sensor to see colors
    if dist_sensor.get_distance_cm() < 15:
        IN Youtube
