# Код избегания препятствий
from spike import DistanceSensor

dist_sensor = DistanceSensor('A')

def avoid_obstacles():
    if dist_sensor.get_distance_cm() < 15:
        # Алгоритм объезда
        pass
